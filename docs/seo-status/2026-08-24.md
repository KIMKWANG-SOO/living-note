# SEO·성장 현황 점검 — 2026-08-24

## 지표 요약

| 항목 | 오늘(08-24) | 직전(08-23) | 변화 |
|---|---|---|---|
| 발행 글 수 | 107편 | 107편 | 변화 없음 |
| 구글 서치콘솔 실측 (최근 28일, 07-24~08-21) | 클릭 29 / 노출 2812 / CTR 1.03% / 평균순위 13.95 | 클릭 29 / 노출 2819 / CTR 1.03% / 평균순위 14.00 | 사실상 동일(27일 겹침 구간 특성), 평균순위 소폭 개선(-0.05) |
| 구글 서치콘솔 실측 (최근 7일, 08-14~08-21, 참고용) | 클릭 2 / 노출 292 / CTR 0.68% / 평균순위 15.93 | 클릭 2 / 노출 727 / CTR 0.28% / 평균순위 13.50 | 노출 큰 폭 감소, 평균순위 상승. 클릭 한 자릿수라 노이즈로 판단(안내대로 클릭·CTR 판단엔 미반영) |
| robots.txt | 정상(200), `Allow: /`, sitemap 경로 명시 | 정상 | 변화 없음 |
| sitemap-index.xml | 정상(200), sitemap-0.xml 1개 (lastmod 2026-08-24) | 정상 (lastmod 08-23) | lastmod 갱신 |
| 인스타그램 | **복구됨** — username ks0814kim, 팔로워 126명, 게시물 120개 | 확인 못 함(토큰 무효화) | 토큰 정상화, 팔로워 +8, 게시물 +6 (08-22 대비) |
| 쓰레드 | 사장 지시로 당분간 점검 보류 | 점검 보류 | - |
| 쿠팡 파트너스 등록 링크 | 17행 (변화 없음), 만료 표시 행 없음 | 17행 | 변화 없음 |
| npm run build | 정상(132페이지) | 정상(129페이지) | +3페이지(정적 자산 포함, 글 수는 동일) |

## 인스타그램 토큰 복구 확인

어제(08-23) 점검에서 `code 190`으로 무효화됐던 인스타그램 액세스 토큰이 오늘 조회에서 정상 응답했다. 사장 쪽에서 재발급했거나 세션이 자연 복구된 것으로 보인다(이 세션에서 조치한 바 없음).

```
{"username":"ks0814kim","followers_count":126,"media_count":120,"id":"27951045721174587"}
```

08-22 마지막 정상 수치(팔로워 118 / 게시물 114) 대비 팔로워 +8, 게시물 +6. 콘텐츠 전략 관련해서는 기존 확인 사실(계정에 생활정보노트와 무관한 개인 릴스 혼재)을 유지하며 추가 제안은 하지 않는다.

## 서치콘솔 상세

28일 창은 지난 점검과 거의 동일(클릭 29, 노출 2812~2819 사이 미세 변동)하다. 페이지별 상위는 `health-insurance-eligibility-certificate-guide`(노출 351)가 계속 1위, `local-development-bond-refund-guide`(클릭9)·`car-ownership-transfer-guide`(클릭8)가 클릭 전환 상위를 유지하고 있다.

7일 창(참고용)에서는 `family-relation-certificate-guide`가 노출 160으로 최상위(검색어 "가족관계증명서" 단독 노출 135, 평균순위 10.90)에 나타난다. 이 페이지는 28일 집계 상위 목록에는 나타나지 않는데, 이는 08-22·08-23 점검에서도 확인된 것과 동일한 스크립트 집계 방식의 특성(`--page` 필터로만 잡히는 불일치)이라 판단하며 추가 조치는 하지 않는다. `car-inspection-complete-guide`(신생 도메인 순위 문제, 08-10 확정)도 동일하게 신규 조치 없음.

새로 눈에 띄는 낮은 순위 페이지는 이번에도 없다 — `unemployment-benefit-guide`(노출 40, 평균순위 29.10)가 지난 점검과 동일 수준으로 유지 중이며, 신생 도메인 순위 문제라는 기존 결론(car-inspection·family-relation 사례로 확정)을 그대로 적용해 개별 재조사는 하지 않는다.

## 쓰레드 — 점검 보류

2026-08-15 사장 지시에 따라 이번 점검에서도 `threads-check.mjs` 실행을 생략했다.

## 쿠팡 파트너스

- 만료일 표시("⚠️...까지") 행 없음, 만료 임박 행도 없음 — 이번 점검에서 삭제/경고 조치 없음.
- 현재 17행, 카테고리 구성 변화 없음.
- 클릭·매출 데이터는 대시보드 접근 권한이 없어 **확인 못 함**.

## 태그 상태

`npm run build`가 132페이지로 정상 완료돼 `ALLOWED_TAGS` z.enum 스키마가 계속 지켜지고 있음을 확인했다.

## 소견

발행 글 수·사이트 기술 상태(robots.txt·sitemap·build) 모두 정상이며, 서치콘솔 28일 창도 지난 점검과 사실상 동일한 수준으로 안정적인 횡보를 이어가고 있다 — 신생 도메인 단계에서는 정상 범위로 판단한다.

이번 점검의 핵심은 **인스타그램 토큰 복구**다. 어제 code 190으로 끊겼던 접근이 오늘 다시 정상화되어 팔로워·게시물 지표를 재확인할 수 있었다(팔로워 126, 게시물 120). 쓰레드는 여전히 사장 지시로 점검 보류 중이다.

**종합 권고**: 발행 파이프라인·사이트 기술 상태·서치콘솔 지표·인스타그램 접근 모두 정상화됐으므로 "지금 이대로 유지"가 적절하다. 쓰레드 계정 복원 관련해서는 사장의 재개 지시를 계속 대기한다.
