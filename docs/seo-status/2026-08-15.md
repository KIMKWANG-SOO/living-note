# SEO·성장 현황 점검 — 2026-08-15

## 지표 요약

| 항목 | 오늘(08-15) | 직전(08-14) | 변화 |
|---|---|---|---|
| 발행 글 수 | 83편 | 77편 | +6편 |
| 구글 서치콘솔 실측 (최근 28일, 07-15~08-12) | 클릭 28 / 노출 2118 / CTR 1.32% / 평균순위 14.45 | 클릭 27 / 노출 1976 / CTR 1.37% / 평균순위 14.21 | 클릭 +1, 노출 +142, CTR -0.05%p, 평균순위 소폭 악화(+0.24) |
| 구글 서치콘솔 실측 (최근 7일, 08-05~08-12, 참고용) | 클릭 18 / 노출 1110 / CTR 1.62% / 평균순위 13.36 | 클릭 19 / 노출 1116 / CTR 1.70% / 평균순위 12.63 | 클릭 -1, 노출 -6, CTR -0.08%p, 평균순위 악화(+0.73) |
| robots.txt | 정상(200), Allow: /, sitemap 경로 명시 | 정상 | 변화 없음 |
| sitemap-index.xml | 정상, sitemap-0.xml 1개 (lastmod 2026-08-15T00:00:00.000Z) | 정상 | 변화 없음 |
| 인스타그램 팔로워 | 108명 | 102명 | +6명 |
| 인스타그램 게시물 수 | 93개 | 90개 | +3개 |
| 쓰레드 팔로워 | **확인 못 함** (아래 참고) | 258명 | - |
| 쿠팡 파트너스 등록 링크 | 17행 (변화 없음) | 17행 | 변화 없음, 만료 표시(⚠️...까지) 행 없음 |
| 태그 상태 | `npm run build` 정상, z.enum 스키마 유지 | 정상 | 별도 이상 없음 |
| npm run build | 정상(105페이지) | 정상(102페이지) | +3페이지 |

## 서치콘솔 상세

28일 창 기준 클릭 27→28(+1), 노출 1976→2118(+142)로 계속 늘고 있으나, 평균순위는 14.21→14.45로 소폭 악화(하락)했다. 7일 창도 클릭 19→18, 평균순위 12.63→13.36으로 함께 악화 방향이라 — 다만 두 창 모두 변동폭이 1 미만이라 아직 추세 반전으로 단정하기는 이르다. 다음 점검에서 같은 방향이 이어지는지 지켜볼 사안.

페이지별로는 `health-insurance-eligibility-certificate-guide`가 28일 노출 1위(338, 클릭 1)로 올라섰고, `local-development-bond-refund-guide`(클릭 9)·`car-ownership-transfer-guide`(클릭 8)가 클릭 전환이 가장 좋다. `family-relation-certificate-guide`는 이번에도 28일 상위 25개 목록에서 빠졌지만 7일 목록에는 노출 213(클릭 0)으로 잡혀 있다 — 08-14 보고서에서 이미 "페이지별 상위 목록은 검색어가 분산된 페이지의 총량을 과소 표시하는 집계 방식 문제"로 원인이 확정된 사안이라 재조사하지 않았다.

검색어별로는 "지역개발채권 환급"(클릭 3)·"지역개발채권 조회"(클릭 2)가 노출 대비 클릭 전환이 가장 좋고, "가족 관계 증명서" 계열 검색어들은 여전히 노출은 많지만 클릭이 거의 없다. car-inspection-complete-guide는 08-10에 원인이 확정된 사안이라 추가 조치를 제안하지 않는다. 노출은 있으나 순위가 낮은 새 페이지는 이번 점검에서 뚜렷이 발견되지 않았다.

## 쓰레드 — 확인 못 함

`node marketing/tools/threads-check.mjs` 실행이 에러로 실패했다. 원문 에러:

```
Error: me: Error validating access token: This Threads account needs to complete a checkpoint before it can access this app. To recover access, log in to www.threads.com and follow the instructions. (code 190)
```

이전까지의 `followersCountError`와 다른, 액세스 토큰 자체가 무효화된 상황이다. **사장이 www.threads.com에 로그인해 계정 체크포인트를 직접 해제해야** 스크립트가 다시 작동한다 — 재시도 로직을 임의로 바꾸지 않았다.

## 쿠팡 파트너스

- 만료일 표시("⚠️...까지") 행 없음, 만료 임박 행도 없음 — 이번 점검에서 삭제/경고 조치 없음.
- 현재 17행, 카테고리 구성 변화 없음.
- 클릭·매출 데이터는 대시보드 접근 권한이 없어 **확인 못 함**.

## 태그 상태

`npm run build`가 105페이지로 정상 완료돼 `ALLOWED_TAGS` z.enum 스키마가 계속 지켜지고 있음을 확인했다. 특정 태그에 글이 1편만 남아있는 경우는 발견되지 않았다.

## 소견

발행 글 수(+6편)와 인스타그램 팔로워(+6명)·게시물(+3개) 모두 꾸준히 늘고 있다. 서치콘솔 28일 클릭·노출은 계속 개선되는 추세지만, 평균순위는 28일·7일 창 모두에서 소폭 악화 방향으로 움직였다 — 아직 노이즈 범위지만 다음 점검에서도 같은 방향이면 원인을 짚어볼 필요가 있다.

**쓰레드 액세스 토큰이 계정 체크포인트로 인해 무효화**돼 이번 점검에서 팔로워 수를 확인하지 못했다. 이건 코드 문제가 아니라 계정 자체의 조치가 필요한 사안이므로, 사장이 www.threads.com에서 체크포인트를 해제해야 다음 점검부터 정상 확인 가능하다.

**종합 권고**: 콘텐츠·인스타 지표는 꾸준히 개선 중이라 "지금 이대로 유지"가 맞다. 다만 (1) 쓰레드 토큰 체크포인트 해제는 사장의 조치가 필요하고, (2) 서치콘솔 평균순위 악화 방향은 다음 점검에서 재확인이 필요하다.
