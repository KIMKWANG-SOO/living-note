# SEO·성장 현황 점검 — 2026-08-19

## 지표 요약

| 항목 | 오늘(08-19) | 직전(08-18) | 변화 |
|---|---|---|---|
| 발행 글 수 | 95편 | 92편 | **+3편** |
| 구글 서치콘솔 실측 (최근 28일, 07-19~08-16) | 클릭 30 / 노출 2834 / CTR 1.06% / 평균순위 14.18 | 클릭 30 / 노출 2800 / CTR 1.07% / 평균순위 14.17 | 클릭 ±0, 노출 +34, CTR -0.01%p, 평균순위 거의 변화없음(+0.01) |
| 구글 서치콘솔 실측 (최근 7일, 08-09~08-16, 참고용) | 클릭 14 / 노출 1308 / CTR 1.07% / 평균순위 13.71 | 클릭 15 / 노출 1369 / CTR 1.10% / 평균순위 13.74 | 클릭 -1, 노출 -61, 평균순위 소폭 개선(-0.03) |
| robots.txt | 정상(200), Allow: /, sitemap 경로 명시 | 정상 | 변화 없음 |
| sitemap-index.xml | 정상, sitemap-0.xml 1개 (lastmod 2026-08-19T00:00:00.000Z) | 정상 (lastmod 08-18) | lastmod 갱신 |
| 인스타그램 팔로워 | 115명 | 118명 | **-3명 (2회 연속 감소)** |
| 인스타그램 게시물 수 | 106개 | 102개 | +4개 |
| 쓰레드 | **점검 보류** (사장 지시, 2026-08-15) | 점검 보류 | - |
| 쿠팡 파트너스 등록 링크 | 17행 (변화 없음) | 17행 | 변화 없음, 만료 표시(⚠️...까지) 행 없음 |
| 태그 상태 | `npm run build` 정상, z.enum 스키마 유지 | 정상 | 별도 이상 없음 |
| npm run build | 정상(117페이지) | 정상(114페이지) | +3페이지 |

## 서치콘솔 상세

28일 창 기준 클릭 30, 노출 2834(+34), CTR·평균순위는 어제와 거의 동일해 뚜렷한 방향 전환은 없다. 7일 창은 클릭이 15→14(-1)로 소폭 줄고 노출도 -61이지만, 7일 창은 클릭 수가 한 자릿수라 노이즈 범위이므로 참고만 한다(평균순위는 오히려 소폭 개선).

페이지별로는 `health-insurance-eligibility-certificate-guide`(28일 노출 351, 클릭1)가 여전히 노출 1위, `local-development-bond-refund-guide`(클릭9)·`car-ownership-transfer-guide`(클릭8)가 클릭 전환 상위를 유지 중이다. `car-inspection-complete-guide`는 기존 진단대로(신생 도메인 순위 문제) 추가 조치 없음.

**데이터 이상 재확인 — `family-relation-certificate-guide` 페이지 미표시**: 이번에도 28일·7일 페이지별 상위-25 목록에 이 페이지가 나타나지 않았다. 그런데 `--page`로 직접 조회하면 28일간 검색어 "가족관계증명서"만으로도 노출 621(클릭 0, 평균순위 10.50)이 있고, 관련 변형 검색어를 합치면 노출 700 이상으로 실제로는 사이트 전체 페이지 중 노출 1위 후보다(현재 목록 1위인 health-insurance 351의 약 2배). 즉 페이지별 집계와 검색어별 집계 사이에 불일치가 존재하며, 08-17/08-18 보고서에서도 같은 현상이 관찰된 바 있다. 노출은 매우 높은데 클릭은 0이고 평균순위가 10.5로 1페이지 진입 direct 근처라는 점은 실제 성과 관점에서 주목할 만하지만, 이는 도구(서치콘솔 API 차원 간 집계 방식) 특성으로 보이며 페이지 자체의 SEO 결함으로 단정할 근거는 없다 — 소제목 등 본문 수정은 제안하지 않는다. 다만 노출 621에 클릭 0이라는 점은 제목 태그(title)·메타 설명이 "가족관계증명서" 검색 의도와 일치하는지 한 번 점검해볼 가치는 있다(추측이므로 강한 권고는 아님).

## 쓰레드 — 점검 보류

2026-08-15 사장 지시에 따라 이번 점검에서도 `threads-check.mjs` 실행을 생략했다.

## 쿠팡 파트너스

- 만료일 표시("⚠️...까지") 행 없음, 만료 임박 행도 없음 — 이번 점검에서 삭제/경고 조치 없음.
- 현재 17행, 카테고리 구성 변화 없음.
- 클릭·매출 데이터는 대시보드 접근 권한이 없어 **확인 못 함**.

## 태그 상태

`npm run build`가 117페이지로 정상 완료돼 `ALLOWED_TAGS` z.enum 스키마가 계속 지켜지고 있음을 확인했다.

## 소견

발행 글 수(+3), 사이트 기술 상태(robots.txt·sitemap·build) 모두 정상. 서치콘솔은 28일 창 기준 어제와 거의 동일해 안정적인 흐름이다.

인스타그램 팔로워가 118→115명으로 또 3명 줄어 2회 연속 감소가 확인됐다. 이미 알려진 대로 이 계정에는 노트와 무관한 사장 개인 콘텐츠가 섞여 있어(2026-08-10 확인) 팔로워 증감을 노트 콘텐츠 성과로 단정하기 어렵지만, 연속 감소가 이어지는 점은 계속 관찰이 필요하다 — 다만 계정 운영 자체는 사장 판단 영역이므로 콘텐츠 전략 변경은 제안하지 않는다.

**종합 권고**: 발행 파이프라인과 사이트 기술 상태는 "지금 이대로 유지". 서치콘솔은 안정적이나 `family-relation-certificate-guide` 페이지의 "고노출·저클릭" 현상은 다음 점검에서도 지속되는지 지켜보고, 반복 확인되면 제목·메타 설명 점검을 한 번 검토할 만하다. 인스타 팔로워는 2회 연속 감소했지만 계정 정체성 문제로 판단되어 별도 조치를 권고하지 않는다. 쓰레드는 사장 지시로 계속 보류 상태.
