# SEO·성장 현황 점검 — 2026-08-14

## 지표 요약

| 항목 | 오늘(08-14) | 직전(08-13) | 변화 |
|---|---|---|---|
| 발행 글 수 | 77편 | 77편 | 변화 없음 |
| 구글 서치콘솔 실측 (최근 28일, 07-14~08-11) | 클릭 27 / 노출 1976 / CTR 1.37% / 평균순위 14.21 | 클릭 25 / 노출 1835 / CTR 1.36% / 평균순위 14.14 | 클릭 +2, 노출 +141, CTR +0.01%p, 평균순위 소폭 하락(0.07↑) |
| 구글 서치콘솔 실측 (최근 7일, 08-04~08-11, 참고용) | 클릭 19 / 노출 1116 / CTR 1.70% / 평균순위 12.63 | 클릭 19 / 노출 1184 / CTR 1.60% / 평균순위 12.95 | 클릭 동일, 노출 -68, CTR +0.10%p, 평균순위 개선(0.32↓) |
| robots.txt | 정상(200), Allow: /, sitemap 경로 명시 | 정상 | 변화 없음 |
| sitemap-index.xml | 정상, sitemap-0.xml 1개 (lastmod 08-14) | 정상 | 변화 없음 |
| 인스타그램 팔로워 | 102명 | 90명 | +12명 |
| 인스타그램 게시물 수 | 90개 | 86개 | +4개 |
| 쓰레드 팔로워 | 258명 | 232명 | +26명 |
| 쿠팡 파트너스 등록 링크 | 17행 (변화 없음) | 17행 | 변화 없음, 만료 표시(⚠️) 행 없음 |
| 태그 상태 | `npm run build` 정상, z.enum 스키마 유지 | 정상 | 별도 이상 없음 |
| npm run build | 정상(102페이지) | 정상(99페이지) | +3페이지 (아래 참고) |

**빌드 페이지 수 참고**: 오늘은 신규 글이 없는데도(77편 동일) 빌드 페이지가 99→102(+3)로 늘었다. 글 수와 무관하게 늘어난 것으로, 원인은 확인하지 않았다(다음 점검 시 반복되면 원인 조사 필요).

## 서치콘솔 상세

28일 창 기준 클릭 25→27(+2), 노출 1835→1976(+141)로 계속 늘고 있고 CTR도 소폭 상승했다. 다만 평균순위는 14.14→14.21로 아주 미세하게 하락(악화)했다 — 변동폭이 0.1 미만이라 노이즈 범위로 판단하며 추세 반전으로 보기는 이르다.

7일 창(08-04~08-11)은 클릭 19로 직전 7일과 동일하고, 노출은 -68이지만 평균순위는 12.95→12.63으로 개선됐다. 28일 추세와 상충하지 않는다.

**데이터 이상 확인 및 원인 규명**: 어제 보고서에서 28일 노출 1위(216회)였던 `family-relation-certificate-guide`가 오늘 28일 "페이지별 노출 상위 25개" 목록에서 통째로 빠졌다. 실제 트래픽이 사라진 것인지 확인하기 위해 `--page` 필터로 이 페이지만 직접 조회한 결과, "가족관계증명서"(노출 131) 등 검색어 25개에 걸쳐 여전히 상당한 노출이 잡히고 있음을 확인했다(합산하면 약 175회 이상, 실제로는 25개로 잘려 더 많을 수 있음). 즉 **실제 노출이 사라진 게 아니라, 페이지별 상위 목록 집계 방식이 검색어가 폭넓게 분산된 페이지의 총량을 과소 표시하는 것으로 보인다** — 스크립트의 "페이지별 상위" 출력은 참고용으로만 보고, 특정 페이지의 정확한 수치가 필요하면 항상 `--page` 필터로 재확인해야 한다.

검색어별로는 "가족관계증명서"(노출 131, 클릭 0)가 여전히 최다 노출·무클릭이고, "지역개발채권 환급"(클릭 3)·"지역개발채권 조회"(클릭 2)가 노출 대비 클릭 전환이 가장 좋다. car-inspection-complete-guide는 08-10에 원인이 "신생 도메인 순위 자체 낮음"으로 확정된 사안이라 이번에도 추가 조치를 제안하지 않는다. 노출은 있으나 순위가 낮은 새 페이지는 이번 점검에서 발견되지 않았다.

## 쿠팡 파트너스

- 만료일 표시("⚠️...까지") 행 없음, 만료 임박 행도 없음 — 이번 점검에서 삭제/경고 조치 없음.
- 현재 17행, 카테고리 구성 변화 없음.
- 클릭·매출 데이터는 대시보드 접근 권한이 없어 **확인 못 함**.

## 태그 상태

`npm run build`가 102페이지로 정상 완료돼 `ALLOWED_TAGS` z.enum 스키마가 계속 지켜지고 있음을 확인했다. 특정 태그에 글이 1편만 남아있는 경우는 발견되지 않았다.

## 소견

서치콘솔 28일 클릭·노출·CTR은 이번에도 개선됐고, 인스타그램(+12명)·쓰레드(+26명) 팔로워 증가세도 이어지고 있다 — 08-12부터 3회 연속 인스타 두 자릿수 증가가 이어지는 중이라 계정 정체성 혼재 이슈와 별개로 팔로워 성장 자체는 견조하다. 쓰레드 +26명은 최근 며칠 중 가장 큰 증가폭이다.

08-11 SEO 점검 공백 이후 이번(08-14)까지 세 차례(08-12·08-13·08-14) 연속 정상 실행됐다 — 공백 패턴은 더 이상 반복되지 않는 것으로 보이나, 계속 관찰한다.

**종합 권고**: 콘텐츠·트래픽·SNS 지표 모두 개선 추세라 "지금 이대로 유지"가 맞다. 다만 페이지별 상위 목록 집계 이상(위 상세 참조)은 데이터 해석 시 유의할 사항으로 기록해둔다.
