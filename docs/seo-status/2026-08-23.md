# SEO·성장 현황 점검 — 2026-08-23

## 지표 요약

| 항목 | 오늘(08-23) | 직전(08-22) | 변화 |
|---|---|---|---|
| 발행 글 수 | 107편 | 101편 | **+6편** |
| 구글 서치콘솔 실측 (최근 28일, 07-23~08-20) | 클릭 29 / 노출 2819 / CTR 1.03% / 평균순위 14.00 | 클릭 29 / 노출 2819 / CTR 1.03% / 평균순위 14.11 | 사실상 동일(27일 겹침 구간 특성), 평균순위만 소폭 개선(-0.11) |
| 구글 서치콘솔 실측 (최근 7일, 08-13~08-20, 참고용) | 클릭 2 / 노출 727 / CTR 0.28% / 평균순위 13.50 | 클릭 3 / 노출 861 / CTR 0.35% / 평균순위 14.19 | 클릭·노출 감소하나 클릭 한 자릿수라 노이즈로 판단(안내대로 클릭·CTR 판단엔 미반영) |
| robots.txt | 정상(200), `Allow: /`, sitemap 경로 명시 | 정상 | 변화 없음 |
| sitemap-index.xml | 정상(200), sitemap-0.xml 1개 (lastmod 2026-08-23) | 정상 (lastmod 08-21) | lastmod 갱신 |
| 인스타그램 | **확인 못 함 — 액세스 토큰 무효화(code 190)** | 팔로워 118명 / 게시물 114개 | 토큰 재발급 전까지 추적 불가 |
| 쓰레드 | 사장 지시로 당분간 점검 보류 | 점검 보류 | - |
| 쿠팡 파트너스 등록 링크 | 17행 (변화 없음), 만료 표시 행 없음 | 17행 | 변화 없음 |
| 태그 상태 | `npm run build` 정상(129페이지), z.enum 스키마 유지 | 정상(123페이지) | +6페이지 |
| npm run build | 정상(129페이지) | 정상(123페이지) | +6페이지 |

## ⚠️ 신규 이슈: 인스타그램 토큰 무효화

`https://graph.instagram.com/v23.0/me` 조회 시 아래 에러로 실패했다:

```
OAuthException (code 190): Error validating access token: The session has been invalidated
because the user changed their password or Facebook has changed the session for security reasons.
```

쓰레드(2026-08-15부터 code 190 "계정 체크포인트" 보류 중)와는 **별개의 세션**이었던 인스타그램 토큰도 이번에 처음으로 무효화된 것을 확인했다. 에러 메시지 자체가 "비밀번호 변경 또는 Facebook 보안 정책"을 원인으로 명시하고 있어, `D:\living-note\.env`의 `INSTAGRAM_ACCESS_TOKEN`을 Meta 개발자 콘솔에서 재발급받아야 한다. 이건 API 키/OAuth 재인증이 필요한 작업이라 이 세션에서 직접 처리할 수 없다 — 사장 확인 및 조치 필요.

## 서치콘솔 상세

28일 창은 지난 점검과 클릭·노출·CTR이 완전히 동일(창 27일 겹침 특성)하고 평균순위만 소폭 개선됐다. 7일 창은 클릭 2·노출 727로 줄었지만 클릭이 한 자릿수라 판단에 반영하지 않는다.

페이지별로는 `health-insurance-eligibility-certificate-guide`(28일 노출 351)가 여전히 노출 1위, `local-development-bond-refund-guide`(클릭9)·`car-ownership-transfer-guide`(클릭8)가 클릭 전환 상위 유지. `car-inspection-complete-guide`(신생 도메인 순위 문제, 08-10 확정)와 `family-relation-certificate-guide`(title·meta 이미 최적화 확인, 08-22 종결)는 기존 결론 그대로이며 추가 조치 없음.

- `family-relation-certificate-guide`를 `--page`로 재조회한 결과 "가족관계증명서" 검색어로 28일 노출 621(평균순위 10.50, 클릭 0)이 그대로 관찰된다. 사이트 전체 페이지별 상위 목록(기본 조회)에는 이 URL이 나타나지 않는데, `--page` 필터로는 잡히는 동일한 불일치가 지난 08-22 점검에서도 있었던 것이라 스크립트 집계 방식의 특성으로 보고 이번에도 별도 조치는 하지 않는다.
- 새로 눈에 띄는 낮은 순위 페이지: `unemployment-benefit-guide`(노출 40, 평균순위 29.10). 다만 car-inspection·family-relation 두 사례에서 이미 "신생 도메인 자체의 순위 문제"라는 구조적 원인이 확인된 만큼, 매 페이지마다 개별 재조사를 반복하기보다 이 패턴이 일반적 현상이라는 결론으로 갈음한다. 필요하면 도메인 신뢰도가 쌓인 뒤 재점검한다.

## 쓰레드 — 점검 보류

2026-08-15 사장 지시에 따라 이번 점검에서도 `threads-check.mjs` 실행을 생략했다.

## 쿠팡 파트너스

- 만료일 표시("⚠️...까지") 행 없음, 만료 임박 행도 없음 — 이번 점검에서 삭제/경고 조치 없음.
- 현재 17행, 카테고리 구성 변화 없음.
- 클릭·매출 데이터는 대시보드 접근 권한이 없어 **확인 못 함**.

## 태그 상태

`npm run build`가 129페이지로 정상 완료돼 `ALLOWED_TAGS` z.enum 스키마가 계속 지켜지고 있음을 확인했다.

## 소견

발행 글 수(+6), 사이트 기술 상태(robots.txt·sitemap·build) 모두 정상. 서치콘솔 28일 창은 지난 점검과 사실상 동일한 수준(클릭 29, 노출 2819)으로 안정적인 횡보 — 신생 도메인 단계에서는 정상 범위로 판단한다.

이번 점검의 핵심은 **인스타그램 액세스 토큰 무효화**다. 쓰레드에 이어 인스타그램까지 API 접근이 끊긴 상태라, SNS 채널 두 곳 모두 자동 점검이 불가능해졌다. 토큰 재발급은 사장이 Meta 개발자 콘솔에서 직접 처리해야 하는 작업이다.

**종합 권고**: 발행 파이프라인·사이트 기술 상태·서치콘솔 지표는 "지금 이대로 유지"가 적절하다. 다만 **인스타그램 토큰 재발급이 필요**하다 — 재발급 전까지는 다음 점검에서도 인스타 지표를 "확인 못 함"으로 보고하게 된다. 쓰레드는 기존과 동일하게 사장 지시로 계속 보류.
