# SEO·성장 현황 점검 — 2026-09-11

## 지표 요약

| 항목 | 오늘(09-11) | 직전 보고(09-10) | 변화 |
|---|---|---|---|
| 발행 글 수 | 164편 | 161편 | +3편 |
| 구글 서치콘솔 실측 (최근 28일, 08-11~09-08) | 클릭 10 / 노출 1188 / CTR 0.84% / 평균순위 13.77 | 클릭 13 / 노출 1326 / CTR 0.98% / 평균순위 13.56 | 클릭 -3, 노출 -138, CTR -0.14%p, 평균순위 +0.21(거의 동일) |
| 구글 서치콘솔 실측 (최근 7일, 09-01~09-08, 참고용) | 클릭 0 / 노출 43 / CTR 0.00% / 평균순위 12.53 | 클릭 1 / 노출 45 / 평균순위 13.78 | 거의 동일(표본 작아 노이즈 큼) |
| robots.txt | 200 정상 | 정상 | 변화 없음 |
| sitemap-index.xml | 200 정상 | 정상 | 변화 없음 |
| 인스타그램 | 팔로워 144명, 게시물 173개 | 팔로워 147, 게시물 170 | 팔로워 -3, 게시물 +3 |
| 쓰레드 | 팔로워 222명 | 팔로워 222 | 변화 없음 |
| 쿠팡 파트너스 등록 링크 | 17행(9개 카테고리) + 배너 1건, 만료 표시 행 없음 | 17행 | 변화 없음 |
| npm run build | 정상(195페이지) | 정상(192페이지) | +3페이지 |

## 서치콘솔 상세

28일 창(08-11~09-08) 클릭10·노출1188로 직전 창(08-10~09-07, 클릭13·노출1326) 대비 소폭 감소했다. CTR·평균순위도 미세하게 나빠졌지만 신생 도메인의 일간 변동 범위 안이라 추세 전환으로 보기는 이르다.

페이지별로는 `local-development-bond-refund-guide`(클릭5, 노출179, 평균순위7.32)가 이번에도 클릭 기준 최상위를 유지했다. `car-inspection-complete-guide`(클릭0, 노출42, 평균순위45.12)는 2026-08-10 확정된 "신생 도메인 순위 문제" 결론 그대로다.

노출은 있는데 순위가 낮은 페이지로 `unemployment-benefit-guide`(노출14, 평균순위46.64)가 이번에도 상위에 올라왔다. 이미 여러 회차에서 반복 등장한 페이지라 `--page`로 검색어 상세를 다시 확인했다:

- "구직 급여"(노출5, 평균54.80), "구직 급여 신청"(노출2, 평균33.50), "실업 급여 조건"(노출2, 평균48.00), "실업급여 신청 조건"(클릭1, 평균34.00) 등

검색어가 본문 표현(실업급여/구직급여 신청 조건·자격)과 이미 정확히 일치하는데도 평균순위가 33~55위(전부 검색결과 3페이지 밖)로 낮다. 즉 `car-inspection-complete-guide`와 동일한 "검색어-소제목 불일치가 아니라 신생 도메인 순위 자체가 낮은" 패턴으로 판단된다. 이 페이지에 대해서도 문구 조정을 제안하지 않는다.

그 외 `driver-license-renewal-guide`(노출10, 평균28.70), `car-tax-prepayment-discount-guide`(노출7, 평균27.14)는 지난 보고서와 수치가 동일해 이번 기간에 새로 노출이 늘지 않았다.

7일 창은 클릭0·노출43으로 표본이 극히 작다. 검색어 상세는 "지역개발채권 환급"(노출14)·"지역개발채권 조회"(노출7)가 여전히 상위로, `local-development-bond-refund-guide`가 검색 유입을 계속 견인 중이다. 28일 창 검색어에서 "가족관계증명서"가 노출 500회로 여전히 압도적 1위·클릭 0건이지만, 이는 2026-09-10에 이미 원인 조사를 마쳤다 — SERP 상위가 정부24·대법원 전자가족관계등록시스템 등 공식 기관으로 채워진 네비게이션 의도 키워드라 민간 블로그가 구조적으로 불리하다. 문구 문제가 아니므로 재조사·재조정을 제안하지 않는다.

## 인스타그램

```
{"username":"ks0814kim","followers_count":144,"media_count":173,"id":"27951045721174587"}
```

09-10(147) 대비 144로 -3 감소했다. "개인 릴스 혼재 중단(2026-08-11)" 이후 한 달 지났지만 여전히 하루 단위로 등락(+3 → -3 등)이 반복되고 있어, 이번 -3 하나로 추세 전환(악화)을 단정하지 않는다.

## 쓰레드

팔로워 222명으로 09-10과 동일, 변화 없음.

## 쿠팡 파트너스

- `marketing/coupang-links.md` 재확인 결과 상품 행 17개(9개 카테고리) + 배너 1건, 09-10 대비 변화 없음.
- "⚠️...까지" 형태의 날짜 기반 만료 표시 행 없음 — 삭제·경고 조치 없음.
- 클릭·매출 데이터는 대시보드 접근 권한이 없어 **확인 못 함**.

## 태그 상태

`npm run build`가 195페이지로 정상 완료돼 `ALLOWED_TAGS` z.enum 스키마가 계속 지켜지고 있음을 확인했다.

## 예약작업 실행 공백 관련

직전 보고서(09-10)와 이번(09-11) 사이 날짜 공백 없음(연속 실행). 09-05~09-07 공백은 사장 확인 결과 휴가로 자리를 비운 기간이었던 것으로 확인됐다(2026-09-11 추가) — 예약작업 인프라 장애가 아니었음.

## 소견

발행 글 수·사이트 기술 상태(robots.txt·sitemap·build) 모두 정상이다. 서치콘솔 지표는 28일·7일 창 모두 직전 대비 소폭 감소했지만 절대치·CTR·평균순위 변화폭이 신생 도메인의 정상적 일간 변동 범위 안이라 우려할 신호는 아니다. `local-development-bond-refund-guide`가 계속 클릭 기준 최상위를 지키고 있다.

`unemployment-benefit-guide`를 `--page`로 직접 조사한 결과, 검색어-본문 불일치가 아니라 신생 도메인 저순위 문제로 확인됐다 — `car-inspection-complete-guide`와 같은 패턴이라 추가 문구 조정은 제안하지 않는다. 인스타그램은 +3→-3으로 등락을 반복해 개인 릴스 혼재 중단의 효과를 아직 판단하기 어렵고, 쓰레드는 변화가 없다.

**종합 권고**: "지금 이대로 유지"가 적절하다. 인스타그램 등락과 서치콘솔 소폭 감소 모두 콘텐츠 전략 변경의 근거가 되기엔 부족하니, 추가 조치 없이 몇 회차 더 지켜본 뒤 추세로 판단할 것을 권한다.
