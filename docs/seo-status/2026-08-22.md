# SEO·성장 현황 점검 — 2026-08-22

## 지표 요약

| 항목 | 오늘(08-22) | 직전(08-20) | 변화 |
|---|---|---|---|
| 발행 글 수 | 101편 | 98편 | **+3편** |
| 구글 서치콘솔 실측 (최근 28일, 07-22~08-19) | 클릭 29 / 노출 2819 / CTR 1.03% / 평균순위 14.11 | 클릭 30 / 노출 2834 / CTR 1.06% / 평균순위 14.18 | 클릭 -1, 노출 -15, 평균순위 소폭 개선(-0.07) — 큰 변화 없음 |
| 구글 서치콘솔 실측 (최근 7일, 08-12~08-19, 참고용) | 클릭 3 / 노출 861 / CTR 0.35% / 평균순위 14.19 | 클릭 8 / 노출 1148 / CTR 0.70% / 평균순위 13.89 | 클릭 -5, 노출 -287 — 7일 창 특성상 노이즈로 판단(클릭 한 자릿수) |
| robots.txt | 정상, `Allow: /`, sitemap 경로 명시 | 정상 | 변화 없음 |
| sitemap-index.xml | 정상, sitemap-0.xml 1개 (lastmod 2026-08-21) | 정상 (lastmod 08-20) | lastmod 갱신 |
| 인스타그램 팔로워 | 118명 | 125명 | **-7명 (감소)** |
| 인스타그램 게시물 수 | 114개 | 110개 | +4개 |
| 쓰레드 | 사장 지시로 당분간 점검 보류 | 점검 보류 | - |
| 쿠팡 파트너스 등록 링크 | 17행 (변화 없음) | 17행 | 변화 없음, 만료 표시(⚠️...까지) 행 없음 |
| 태그 상태 | `npm run build` 정상, z.enum 스키마 유지 | 정상 | 별도 이상 없음 |
| npm run build | 정상(123페이지) | 정상(120페이지) | +3페이지 |

## 서치콘솔 상세

28일·7일 창 모두 지난 점검(08-20) 대비 눈에 띄는 변화는 없다. 28일 창은 클릭 -1, 노출 -15로 사실상 횡보. 7일 창은 클릭·노출 모두 줄었지만 클릭이 한 자릿수라 안내대로 판단에 반영하지 않는다.

페이지별로는 `health-insurance-eligibility-certificate-guide`(28일 노출 351, 클릭1)가 여전히 노출 1위, `local-development-bond-refund-guide`(클릭9)·`car-ownership-transfer-guide`(클릭8)가 클릭 전환 상위를 유지 중이다. `car-inspection-complete-guide`는 기존 진단대로(신생 도메인 순위 문제) 추가 조치 없음.

**`family-relation-certificate-guide` 후속 조사 결과 (직전 보고서에서 예고한 확인)**: 08-20 보고서에서 이 페이지의 실제 title·meta description을 열어 "가족관계증명서" 검색 의도와 일치하는지 확인하기로 했던 건을 오늘 실행했다. 실제 파일(`src/content/blog/family-relation-certificate-guide.md`)을 열어보니 title은 `'가족관계증명서 인터넷 발급 방법 총정리 — 정부24·대법원 안내'`, description도 "가족관계증명서(일반·상세·특정)", "인터넷 발급" 등 핵심 검색어를 이미 정확히 포함하고 있다. 즉 **title·meta는 검색 의도와 이미 잘 일치하며, 문구 불일치가 원인이 아니라는 것이 확인됐다.** `--page`로 다시 조회해도 "가족관계증명서" 검색어 하나로 28일 노출 621(평균순위 10.50, 클릭 0)이 여전히 관찰된다. 평균순위 10위권은 검색결과 1페이지 하단~2페이지 상단에 해당해 실제 클릭률이 원래 낮은 구간인 데다, car-inspection 사례와 마찬가지로 신생 도메인이라 신뢰도(브랜드 인지) 기반 클릭이 붙지 않는 구조적 요인일 가능성이 높다. **문구 수정은 근거가 없으므로 제안하지 않으며, 이 건은 여기서 결론 내리고 추가 재조사는 필요 시에만 진행한다.**

## 쓰레드 — 점검 보류

2026-08-15 사장 지시에 따라 이번 점검에서도 `threads-check.mjs` 실행을 생략했다.

## 쿠팡 파트너스

- 만료일 표시("⚠️...까지") 행 없음, 만료 임박 행도 없음 — 이번 점검에서 삭제/경고 조치 없음.
- 현재 17행, 카테고리 구성 변화 없음.
- 클릭·매출 데이터는 대시보드 접근 권한이 없어 **확인 못 함**.

## 태그 상태

`npm run build`가 123페이지로 정상 완료돼 `ALLOWED_TAGS` z.enum 스키마가 계속 지켜지고 있음을 확인했다.

## 소견

발행 글 수(+3), 사이트 기술 상태(robots.txt·sitemap·build) 모두 정상. 서치콘솔 28일 창은 지난 점검과 거의 동일한 수준(클릭 29, 노출 2819)으로 안정적인 횡보를 보인다 — 신생 도메인 단계에서는 정상 범위로 판단한다.

인스타그램 팔로워가 125→118명으로 7명 감소했다. 게시물은 4개 늘었는데 팔로워는 줄었다는 점은 다소 특이하지만, 이 계정에 생활정보노트와 무관한 사장 개인 콘텐츠가 섞여 있는 구조적 문제(2026-08-10 확인)를 감안하면 팔로워 증감이 노트 콘텐츠 성과를 그대로 반영하는 지표는 아니라고 판단된다. 반복적인 콘텐츠 전략 제안은 하지 않는다.

`family-relation-certificate-guide`는 이번 조사로 title·meta 불일치가 원인이 아님이 확인되어 이 건은 종결한다.

**종합 권고**: 발행 파이프라인·사이트 기술 상태·서치콘솔 지표 모두 "지금 이대로 유지"가 적절하다. 인스타 팔로워 감소는 계정 정체성 문제로 판단이 어려워 별도 조치를 권고하지 않는다. 쓰레드는 사장 지시로 계속 보류 상태.
