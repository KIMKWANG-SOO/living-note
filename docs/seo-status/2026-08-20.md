# SEO·성장 현황 점검 — 2026-08-20

## 지표 요약

| 항목 | 오늘(08-20) | 직전(08-19) | 변화 |
|---|---|---|---|
| 발행 글 수 | 98편 | 95편 | **+3편** |
| 구글 서치콘솔 실측 (최근 28일, 07-20~08-17) | 클릭 30 / 노출 2834 / CTR 1.06% / 평균순위 14.18 | 클릭 30 / 노출 2834 / CTR 1.06% / 평균순위 14.18 | **완전 동일** (아래 서치콘솔 상세 참고) |
| 구글 서치콘솔 실측 (최근 7일, 08-10~08-17, 참고용) | 클릭 8 / 노출 1148 / CTR 0.70% / 평균순위 13.89 | 클릭 14 / 노출 1308 / CTR 1.07% / 평균순위 13.71 | 클릭 -6, 노출 -160, 평균순위 소폭 하락(+0.18) — 노이즈 범위 |
| robots.txt | 정상(200 추정), `Allow: /`, sitemap 경로 명시 | 정상 | 변화 없음 |
| sitemap-index.xml | 정상, sitemap-0.xml 1개 (lastmod 2026-08-20T00:00:00.000Z) | 정상 (lastmod 08-19) | lastmod 갱신 |
| 인스타그램 팔로워 | **125명** | 115명 | **+10명 (반등)** |
| 인스타그램 게시물 수 | 110개 | 106개 | +4개 |
| 쓰레드 | **점검 보류** (사장 지시, 2026-08-15) | 점검 보류 | - |
| 쿠팡 파트너스 등록 링크 | 17행 (변화 없음) | 17행 | 변화 없음, 만료 표시(⚠️...까지) 행 없음 |
| 태그 상태 | `npm run build` 정상, z.enum 스키마 유지 | 정상 | 별도 이상 없음 |
| npm run build | 정상(120페이지) | 정상(117페이지) | +3페이지 |

## 서치콘솔 상세

**28일 창 수치가 어제 보고서와 완전히 동일하다** (클릭 30 / 노출 2834 / CTR 1.06% / 평균순위 14.18 — 소수점까지 일치). 조회 기간 자체는 07-19~08-16(어제)에서 07-20~08-17(오늘)로 하루 밀렸는데 총합이 똑같다는 것은, 서치콘솔 API의 최신 데이터 반영에 통상 2~3일의 지연이 있어 이번 조회에도 실질적으로 새로 반영된 날짜 데이터가 없었을 가능성이 높다는 뜻이다. 우려할 이상 신호는 아니고 도구 특성으로 판단한다.

7일 창은 클릭 14→8, 노출 1308→1148로 감소했지만, 안내대로 7일 창은 클릭 수가 한 자릿수라 노이즈 범위이므로 판단에 반영하지 않는다.

페이지별로는 `health-insurance-eligibility-certificate-guide`(28일 노출 351, 클릭1)가 여전히 노출 1위, `local-development-bond-refund-guide`(클릭9)·`car-ownership-transfer-guide`(클릭8)가 클릭 전환 상위를 유지 중이다. `car-inspection-complete-guide`는 기존 진단대로(신생 도메인 순위 문제) 추가 조치 없음.

**`family-relation-certificate-guide` 고노출·저클릭 현상 — 4일 연속 관찰**: 이번에도 28일·7일 페이지별 상위-25 목록에 이 페이지가 나타나지 않지만, `--page`로 직접 조회하면 28일간 "가족관계증명서" 단일 검색어만으로 노출 621(클릭 0, 평균순위 10.50)이 잡힌다. 7일 창에서는 아예 노출 1위(626)로 집계 자체에는 나타났다. 08-17부터 오늘까지 최소 4회 연속 같은 패턴이 관찰되는데, 평균순위 10.5는 1페이지 근처인데도 클릭이 0이라는 점은 재현성 있는 신호로 볼 수 있다. 다만 이는 검색어-소제목 불일치 문제로 단정할 근거가 없고(car-inspection 사례처럼 성급한 소제목 수정을 반복하지 말라는 원칙 유지), 08-19 보고서에서 이미 "제목 태그·메타 설명 점검을 검토할 만하다"는 낮은 확신의 제안을 했다. 같은 제안을 또 반복하기보다, 다음 점검에서 이 페이지의 실제 `<title>`과 meta description을 직접 열어 "가족관계증명서" 검색 의도와 얼마나 일치하는지 확인하는 조사를 권한다(이번 점검에서는 시간상 생략).

## 쓰레드 — 점검 보류

2026-08-15 사장 지시에 따라 이번 점검에서도 `threads-check.mjs` 실행을 생략했다.

## 쿠팡 파트너스

- 만료일 표시("⚠️...까지") 행 없음, 만료 임박 행도 없음 — 이번 점검에서 삭제/경고 조치 없음.
- 현재 17행, 카테고리 구성 변화 없음.
- 클릭·매출 데이터는 대시보드 접근 권한이 없어 **확인 못 함**.

## 태그 상태

`npm run build`가 120페이지로 정상 완료돼 `ALLOWED_TAGS` z.enum 스키마가 계속 지켜지고 있음을 확인했다.

## 소견

발행 글 수(+3), 사이트 기술 상태(robots.txt·sitemap·build) 모두 정상. 서치콘솔 28일 창은 어제와 완전 동일한 수치였는데, 이는 API 반영 지연으로 판단되며 별도 조치는 불필요하다.

인스타그램 팔로워가 115→125명으로 10명 늘며 2회 연속 감소세가 반전됐다. 이 계정에 노트와 무관한 사장 개인 콘텐츠가 섞여 있는 점(2026-08-10 확인)을 감안하면 이 증가가 노트 콘텐츠 성과인지는 단정하기 어렵지만, 최소한 우려할 방향은 아니다.

**종합 권고**: 발행 파이프라인과 사이트 기술 상태는 "지금 이대로 유지". `family-relation-certificate-guide`의 고노출·저클릭 현상이 4일째 재현되고 있어, 다음 점검 전에 한 번은 이 페이지의 실제 title·meta description을 열어보고 "가족관계증명서" 검색 의도와의 일치 여부를 확인해볼 것을 권한다(단, 이는 실측 확인 후 판단할 사안이며 지금 바로 본문을 수정하라는 뜻은 아니다). 인스타 팔로워는 반등했으나 계정 정체성 문제로 판단이 어려워 특별한 조치는 권고하지 않는다. 쓰레드는 사장 지시로 계속 보류 상태.
