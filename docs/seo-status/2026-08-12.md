# SEO·성장 현황 점검 — 2026-08-12

## 지표 요약

| 항목 | 오늘(08-12) | 직전(08-10) | 변화 |
|---|---|---|---|
| 발행 글 수 | 74편 | 69편 | +5편 (08-11 미발행 2편 소급발행 + 08-12 정상 3편) |
| 구글 서치콘솔 실측 (최근 28일, 07-12~08-09) | 클릭 22 / 노출 1686 / CTR 1.30% / 평균순위 14.38 | 클릭 15 / 노출 1431 / CTR 1.05% / 평균순위 14.58 | 클릭 +7, 노출 +255, CTR +0.25%p, 평균순위 개선(0.20↓) |
| 구글 서치콘솔 실측 (최근 7일, 08-02~08-09, 참고용) | 클릭 18 / 노출 1240 / CTR 1.45% / 평균순위 12.92 | 비교 불가(08-11 보고서 공백으로 직전 7일 수치 없음) | - |
| robots.txt | 정상(200), Allow: /, sitemap 경로 명시 | 정상 | 변화 없음 |
| sitemap-index.xml | 정상, sitemap-0.xml 1개 (lastmod 08-12) | 정상 | 변화 없음 |
| 인스타그램 팔로워 | 74명 | 61명 | +13명 |
| 인스타그램 게시물 수 | 83개 | 77개 | +6개 |
| 쓰레드 팔로워 | 222명 | 189명 | +33명 |
| 쿠팡 파트너스 등록 링크 | 17행 (변화 없음) | 17행 | 변화 없음, 만료 표시(⚠️) 행 없음 |
| 태그 상태 | 16개 고정 태그 전부 사용, 최소 사용 태그도 2편 이상(반려동물 2편) | - | 1편짜리 태그 없음, 재증식 없음 |
| npm run build | 정상(96페이지) | 정상(122페이지) | -26페이지(08-11 태그 47→16 통합 조치, 커밋 e61ac8b 반영분 — 이미 알려진 정상 원인) |

**⚠️ 예약작업 공백 재발**: `docs/seo-status/2026-08-11.md`가 없음 — 08-11에 blog 발행(daily-post)은 정상 실행됐지만(커밋 4d4ebe2, e61ac8b는 문서/코드 수정 커밋일 뿐 SEO 점검 보고서 커밋은 없음) SEO 점검만 누락됨. `list_scheduled_tasks`로 확인한 `living-note-seo-check`의 cron은 `0 9 * * *`(매일 09:10)로 정상 설정돼 있고 `lastRunAt`도 갱신되는 중이지만, 기존에 확인된 대로 `lastRunAt` 갱신이 실제 실행/완료를 보장하지 않는 패턴([[scheduled-task-preflight-check]])이라 이번 공백의 근본 원인은 이번 점검만으로는 특정하지 못함. 08-07·08-08에 이어 세 번째 공백 — 반복되는 패턴이므로 인프라 자체(세션 생성 실패 여부 등) 점검이 필요한 시점으로 보임.

## 서치콘솔 상세

28일 창 기준 클릭 15→22(+7), 노출 1431→1686(+255), 평균순위 14.58→14.38로 전 지표가 개선됐다. CTR도 1.05%→1.30%로 함께 올라, 노출만 늘고 클릭이 안 느는 이전 패턴과 달리 이번엔 클릭 전환도 함께 좋아졌다.

노출 최상위는 이번엔 `family-relation-certificate-guide`(노출 182, 평균 12.03위)이고, `car-inspection-complete-guide`(노출 165, 평균 42.16위)가 근소한 차이로 2위다. car-inspection 페이지는 2026-08-10에 확정한 대로 검색어-소제목은 이미 일치하며 순위 자체가 낮은 것이 원인(신생 도메인 특성)으로 결론난 사안이라 이번에도 추가 조치를 제안하지 않는다. 그 외 상위 노출 페이지들(`car-insurance-direct-guide` 7.61위, `local-development-bond-refund-guide` 6.19위 등)은 노출 대비 순위가 이미 양호해 "노출은 많은데 순위가 낮은 신규 페이지" 패턴에 해당하는 곳이 없었다 — 이번엔 문구 조정 제안 없음.

7일 창(08-02~08-09)은 클릭 18·노출 1240·평균 12.92위로, 28일 창보다 순위가 더 좋게 나온다(최근 며칠 추세가 개선 중임을 시사). 다만 08-11 보고서가 없어 직전 7일 수치와 나란히 비교할 기준점이 없다 — 다음 점검부터는 정상적으로 전날 대비 비교가 가능할 것으로 보인다.

검색어별로는 "가족관계증명서"(노출 98, 클릭 0)가 여전히 최다 노출·무클릭이고, "지역개발채권 환급"(노출 17, 클릭 3)이 노출 대비 클릭 전환이 가장 좋다.

## 쿠팡 파트너스

- 만료일 표시("⚠️...까지") 행 없음, 만료 임박 행도 없음 — 이번 점검에서 삭제/경고 조치 없음.
- 현재 17행, 카테고리 구성 변화 없음.
- 클릭·매출 데이터는 대시보드 접근 권한이 없어 **확인 못 함**.

## 태그 상태

`npm run build`가 96페이지로 정상 완료돼 `ALLOWED_TAGS` z.enum 스키마가 계속 지켜지고 있음을 확인했다. 태그별 글 수를 세어보니 16개 전부 실사용 중이고 최소 사용 태그(반려동물)도 2편이라 1편짜리 얇은 태그는 없다 — 08-11 조치(태그 47→16 통합 + 스키마 강제) 이후 재증식 없이 유지되고 있다.

## 소견

서치콘솔·인스타그램·쓰레드 세 지표 모두 이번 점검에서 뚜렷하게 개선됐다(클릭·노출·CTR·평균순위 전부 동반 상승, 인스타 +13명, 쓰레드 +33명). 특히 인스타그램은 그동안 몇 차례 점검에서 정체가 이어지던 것과 달리 이번엔 게시물 6개 추가에 팔로워 13명이 늘어 이전보다 반응이 좋아진 것으로 보이나, 계정에 개인 콘텐츠가 섞여 있는 기존 확인 사실([[living-note-blog]] 참고)을 고려하면 이 증가분이 생활정보노트 콘텐츠 덕분인지는 단정하기 어렵다 — 반복 제안 없이 추세만 기록한다.

가장 주의가 필요한 부분은 SEO 점검 예약작업 자체의 세 번째 공백(08-07·08-08·08-11)이다. 콘텐츠·수치 지표는 전부 양호하니, 다음 사이클에서는 이 예약작업이 왜 간헐적으로 누락되는지(세션 생성 실패, 트리거 문제 등) 원인 조사가 필요해 보인다.

**종합 권고**: 콘텐츠·트래픽 지표는 전반적으로 개선 추세라 "지금 이대로 유지"가 맞다. 다만 SEO 점검 예약작업의 반복되는 무음 공백은 더 미루지 말고 다음 기회에 원인을 조사하는 것을 권한다.
