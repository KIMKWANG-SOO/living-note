# SEO·성장 현황 점검 — 2026-08-17

## 지표 요약

| 항목 | 오늘(08-17) | 직전(08-16) | 변화 |
|---|---|---|---|
| 발행 글 수 | 86편 | 86편 | 변화 없음 |
| 구글 서치콘솔 실측 (최근 28일, 07-17~08-14) | 클릭 30 / 노출 2735 / CTR 1.10% / 평균순위 13.85 | 클릭 28 / 노출 2554 / CTR 1.10% / 평균순위 14.01 | 클릭 +2, 노출 +181, CTR ±0, 평균순위 개선(-0.16) |
| 구글 서치콘솔 실측 (최근 7일, 08-07~08-14, 참고용) | 클릭 15 / 노출 1378 / CTR 1.09% / 평균순위 13.14 | 클릭 15 / 노출 1350 / CTR 1.11% / 평균순위 13.11 | 클릭 ±0, 노출 +28, 평균순위 거의 변화 없음(+0.03) |
| robots.txt | 정상(200), Allow: /, sitemap 경로 명시 | 정상 | 변화 없음 |
| sitemap-index.xml | 정상, sitemap-0.xml 1개 (lastmod 2026-08-17T00:00:00.000Z) | 정상 (lastmod 08-16) | lastmod 갱신 |
| 인스타그램 팔로워 | 121명 | 117명 | +4명 |
| 인스타그램 게시물 수 | 98개 | 96개 | +2개 |
| 쓰레드 | **점검 보류** (사장 지시, 2026-08-15) | 점검 보류 | - |
| 쿠팡 파트너스 등록 링크 | 17행 (변화 없음) | 17행 | 변화 없음, 만료 표시(⚠️...까지) 행 없음 |
| 태그 상태 | `npm run build` 정상, z.enum 스키마 유지 | 정상 | 별도 이상 없음 |
| npm run build | 정상(111페이지) | 정상(108페이지) | +3페이지 (글 수는 그대로인데 페이지 수만 증가 — 원인 특정 안 함, 빌드 자체는 정상) |

## 서치콘솔 상세

28일 창 기준 클릭 28→30(+2), 노출 2554→2735(+181), 평균순위 14.01→13.85로 소폭 개선됐다. 7일 창은 클릭 동일(15), 노출 소폭 증가(+28), 평균순위는 사실상 변화 없음(+0.03) — 전반적으로 완만한 우상향이 이어지는 흐름이다.

페이지별로는 `local-development-bond-refund-guide`(클릭 9)·`car-ownership-transfer-guide`(클릭 8)가 여전히 클릭 전환 상위이며, `health-insurance-eligibility-certificate-guide`가 28일 노출 1위(349, 클릭 1)를 유지 중이다.

**`family-relation-certificate-guide` 관련 데이터 이상 확인**: 이 페이지는 7일 창에서 노출 644(28일 창 페이지별 상위 25개 목록에는 나타나지 않음)로 계속 큰 볼륨을 유지 중이다. `--page`로 직접 조회한 결과 28일 누적 노출은 705(핵심 검색어 "가족관계증명서" 619, 평균순위 10.36 포함), 클릭은 여전히 0이다. 이 진단은 08-16 점검에서 이미 완료됐다 — 제목에 검색어가 그대로 포함돼 있어 문구 불일치 문제가 아니라 순위(10위 전후, 검색결과 1페이지 최하단권) 자체가 원인이며, 신생 도메인 특성상 시간이 지나야 개선될 사안이다. 따라서 추가 문구 수정을 제안하지 않는다. (참고: 이 페이지가 28일 창 상위-25 페이지 목록 출력에는 표시되지 않고 7일 창·`--page` 조회에서만 보이는 현상이 이틀 연속 관찰됨 — 스크립트의 페이지별 목록 산출 로직에 표시 누락 가능성이 있어 보이나, 이번 점검 범위를 벗어나 원인 규명은 하지 않았다.)

노출은 있으나 순위가 낮은 **새** 페이지는 이번 점검에서 별도로 발견되지 않았다.

## 쓰레드 — 점검 보류

2026-08-15 사장 지시에 따라 이번 점검에서도 `threads-check.mjs` 실행을 생략했다. 사장의 재개 지시가 있을 때까지 계속 보류한다.

## 쿠팡 파트너스

- 만료일 표시("⚠️...까지") 행 없음, 만료 임박 행도 없음 — 이번 점검에서 삭제/경고 조치 없음.
- 현재 17행, 카테고리 구성 변화 없음.
- 클릭·매출 데이터는 대시보드 접근 권한이 없어 **확인 못 함**.

## 태그 상태

`npm run build`가 111페이지로 정상 완료돼 `ALLOWED_TAGS` z.enum 스키마가 계속 지켜지고 있음을 확인했다. 특정 태그에 글이 1편만 남아있는 경우는 발견되지 않았다.

## 소견

발행 글 수가 08-16과 동일한 86편으로, 지난 며칠간의 +2~3편 증가 흐름이 이번 점검 시점 기준으로는 멈춰 있다 — 발행 파이프라인이 정상 작동 중인지 확인이 필요할 수 있다. 다만 npm run build 페이지 수는 108→111로 늘어난 걸 보면 콘텐츠 자체는 정지된 게 아닐 가능성도 있다.

서치콘솔은 28일·7일 창 모두 완만한 개선세를 유지 중이며 우려할 신호는 없다. 인스타그램도 팔로워·게시물 모두 순조롭게 증가했다.

`family-relation-certificate-guide`는 여전히 순위·도메인 신뢰도 문제로 클릭 0인 상태이며 이미 원인 진단이 끝났으므로 추가 조치는 시간에 맡긴다.

**종합 권고**: 서치콘솔·인스타 지표는 "지금 이대로 유지"가 맞다. 다만 오늘 발행 글 수가 정체된 점은 사장이 직접 확인해볼 가치가 있다. 쓰레드는 사장 지시로 계속 보류 상태.
