# SEO·성장 현황 점검 — 2026-08-18

## 지표 요약

| 항목 | 오늘(08-18) | 직전(08-17) | 변화 |
|---|---|---|---|
| 발행 글 수 | 92편 | 86편 | **+6편** |
| 구글 서치콘솔 실측 (최근 28일, 07-18~08-15) | 클릭 30 / 노출 2800 / CTR 1.07% / 평균순위 14.17 | 클릭 30 / 노출 2735 / CTR 1.10% / 평균순위 13.85 | 클릭 ±0, 노출 +65, CTR -0.03%p, 평균순위 소폭 하락(+0.32) |
| 구글 서치콘솔 실측 (최근 7일, 08-08~08-15, 참고용) | 클릭 15 / 노출 1369 / CTR 1.10% / 평균순위 13.74 | 클릭 15 / 노출 1378 / CTR 1.09% / 평균순위 13.14 | 클릭 ±0, 노출 -9, 평균순위 소폭 하락(+0.60) |
| robots.txt | 정상(200), Allow: /, sitemap 경로 명시 | 정상 | 변화 없음 |
| sitemap-index.xml | 정상, sitemap-0.xml 1개 (lastmod 2026-08-18T00:00:00.000Z) | 정상 (lastmod 08-17) | lastmod 갱신 |
| 인스타그램 팔로워 | 118명 | 121명 | **-3명** |
| 인스타그램 게시물 수 | 102개 | 98개 | +4개 |
| 쓰레드 | **점검 보류** (사장 지시, 2026-08-15) | 점검 보류 | - |
| 쿠팡 파트너스 등록 링크 | 17행 (변화 없음) | 17행 | 변화 없음, 만료 표시(⚠️...까지) 행 없음 |
| 태그 상태 | `npm run build` 정상, z.enum 스키마 유지 | 정상 | 별도 이상 없음 |
| npm run build | 정상(114페이지) | 정상(111페이지) | +3페이지 |

## 서치콘솔 상세

28일 창 기준 클릭은 30으로 동일하지만 노출이 2735→2800(+65)로 늘었고, CTR은 1.10%→1.07%(-0.03%p), 평균순위는 13.85→14.17로 소폭 하락(악화)했다. 7일 창도 노출이 살짝 줄고(-9) 평균순위가 소폭 하락(+0.60)해 같은 방향이다. 다만 두 지표 모두 변동폭이 크지 않고 클릭 수 자체는 유지되고 있어, 노이즈 범위 내로 보이며 아직 추세 전환이라 판단하긴 이르다.

페이지별로는 `local-development-bond-refund-guide`(클릭 9)·`car-ownership-transfer-guide`(클릭 8)가 여전히 클릭 전환 상위이며, `health-insurance-eligibility-certificate-guide`가 28일 노출 1위(351, 클릭 1)를 유지 중이다.

`car-inspection-complete-guide`는 이미 원인 진단이 끝난 사안(신생 도메인 순위 문제, 소제목 문제 아님)이라 추가 조치를 제안하지 않는다.

이번 점검의 28일 창 페이지별 상위-25 목록에는 `family-relation-certificate-guide`가 표시되지 않았고, 7일 창에도 나타나지 않았다(어제 보고서에서 언급된 표시 누락 현상이 이번엔 발생하지 않은 것으로 보인다 — 별도 `--page` 조회는 이번 점검 범위에서 생략). 노출은 있으나 순위가 낮은 **새** 페이지는 이번 점검에서 별도로 발견되지 않았다.

## 쓰레드 — 점검 보류

2026-08-15 사장 지시에 따라 이번 점검에서도 `threads-check.mjs` 실행을 생략했다. 사장의 재개 지시가 있을 때까지 계속 보류한다.

## 쿠팡 파트너스

- 만료일 표시("⚠️...까지") 행 없음, 만료 임박 행도 없음 — 이번 점검에서 삭제/경고 조치 없음.
- 현재 17행, 카테고리 구성 변화 없음.
- 클릭·매출 데이터는 대시보드 접근 권한이 없어 **확인 못 함**.

## 태그 상태

`npm run build`가 114페이지로 정상 완료돼 `ALLOWED_TAGS` z.enum 스키마가 계속 지켜지고 있음을 확인했다. 특정 태그에 글이 1편만 남아있는 경우는 발견되지 않았다.

## 소견

발행 글 수가 86→92편(+6)으로 다시 크게 늘어 어제 지적했던 "발행 정체" 우려는 해소된 것으로 보인다. 사이트 기술 상태(robots.txt, sitemap, build)도 모두 정상이다.

서치콘솔은 28일·7일 창 모두 노출·평균순위가 소폭 뒷걸음질쳤지만 클릭 수는 유지되고 있어, 아직은 신생 도메인의 정상적인 변동 범위로 보인다 — 다만 이 하락이 며칠 더 이어지는지는 다음 점검에서 계속 지켜볼 필요가 있다.

인스타그램은 게시물 수는 +4개로 늘었지만 팔로워가 121→118명으로 3명 감소했다. 팔로워 수가 줄어든 건 최근 점검 이력에서 처음 관찰되는 방향 전환이다. 다만 이미 확인된 대로 이 계정에는 생활정보노트와 무관한 사장 개인 콘텐츠가 섞여 있어(2026-08-10 확인) 팔로워 증감이 노트 콘텐츠 성과와 직접 연결된다고 단정하기 어렵다 — 콘텐츠 전략 변경을 제안하지 않고 사실만 기록한다.

**종합 권고**: 발행 파이프라인과 사이트 기술 상태는 "지금 이대로 유지"가 맞다. 서치콘솔 소폭 하락과 인스타 팔로워 감소는 아직 조치가 필요한 수준은 아니나, 다음 1~2회 점검에서 같은 방향이 반복되는지 확인이 필요하다. 쓰레드는 사장 지시로 계속 보류 상태.
