# SEO·성장 현황 점검 — 2026-08-16

## 지표 요약

| 항목 | 오늘(08-16) | 직전(08-15) | 변화 |
|---|---|---|---|
| 발행 글 수 | 86편 | 83편 | +3편 |
| 구글 서치콘솔 실측 (최근 28일, 07-16~08-13) | 클릭 28 / 노출 2554 / CTR 1.10% / 평균순위 14.01 | 클릭 28 / 노출 2118 / CTR 1.32% / 평균순위 14.45 | 클릭 ±0, 노출 +436, CTR -0.22%p, 평균순위 개선(-0.44) |
| 구글 서치콘솔 실측 (최근 7일, 08-06~08-13, 참고용) | 클릭 15 / 노출 1350 / CTR 1.11% / 평균순위 13.11 | 클릭 18 / 노출 1110 / CTR 1.62% / 평균순위 13.36 | 클릭 -3, 노출 +240, 평균순위 소폭 개선(-0.25) |
| robots.txt | 정상(200), Allow: /, sitemap 경로 명시 | 정상 | 변화 없음 |
| sitemap-index.xml | 정상, sitemap-0.xml 1개 (lastmod 2026-08-16T00:00:00.000Z) | 정상 (lastmod 08-15) | lastmod 갱신 |
| 인스타그램 팔로워 | 117명 | 108명 | +9명 |
| 인스타그램 게시물 수 | 96개 | 93개 | +3개 |
| 쓰레드 | **점검 보류** (사장 지시, 2026-08-15) | 확인 못 함(토큰 체크포인트) | - |
| 쿠팡 파트너스 등록 링크 | 17행 (변화 없음) | 17행 | 변화 없음, 만료 표시(⚠️...까지) 행 없음 |
| 태그 상태 | `npm run build` 정상, z.enum 스키마 유지 | 정상 | 별도 이상 없음 |
| npm run build | 정상(108페이지) | 정상(105페이지) | +3페이지 |

## 서치콘솔 상세

28일 창 기준 클릭은 28로 동일하지만 노출이 2118→2554(+436)로 크게 늘었고, 평균순위도 14.45→14.01로 개선됐다. 7일 창은 클릭이 18→15로 줄었지만 노출은 1110→1350(+240)으로 늘고 평균순위도 13.36→13.11로 개선됐다 — 클릭 감소는 7일 창 특성상 노이즈로 보이며, 28일 기준 판단 원칙대로 "클릭 정체·노출 증가·순위 개선"을 주된 신호로 본다.

페이지별로는 `local-development-bond-refund-guide`(클릭 9)·`car-ownership-transfer-guide`(클릭 8)가 여전히 클릭 전환 상위이며, `health-insurance-eligibility-certificate-guide`가 28일 노출 1위(347)를 유지 중이다.

**`family-relation-certificate-guide` 재확인**: 7일 창 노출 1위(525, 클릭 0, 평균순위 11.86)로 계속 커지고 있어 `--page`로 실제 검색어를 직접 확인했다. 28일 기준 핵심 검색어 "가족관계증명서"가 노출 486 / 평균순위 10.39인데 클릭이 0이다. 글 제목("가족관계증명서 인터넷 발급 방법 총정리 — 정부24·대법원 안내")에 이미 검색어가 그대로 들어가 있어 제목-검색어 불일치 문제는 아니다 — 순위 10위 전후는 검색결과 1페이지 최하단권이라 신생 도메인 특성상 CTR이 자연히 낮게 나오는 구간으로 보인다(08-10 `car-inspection-complete-guide` 건과 같은 패턴: 원인은 문구가 아니라 도메인 신뢰도·순위 자체). 따라서 이 페이지에도 소제목 수정을 제안하지 않는다 — 시간에 따른 도메인 신뢰도 축적을 기다리는 것이 맞다.

노출은 있으나 순위가 낮은 **새** 페이지는 이번 점검에서 별도로 발견되지 않았다.

## 쓰레드 — 점검 보류

2026-08-15 사장 지시에 따라 이번 점검에서 `threads-check.mjs` 실행을 생략했다. 사장의 재개 지시가 있을 때까지 계속 보류한다.

## 쿠팡 파트너스

- 만료일 표시("⚠️...까지") 행 없음, 만료 임박 행도 없음 — 이번 점검에서 삭제/경고 조치 없음.
- 현재 17행, 카테고리 구성 변화 없음.
- 클릭·매출 데이터는 대시보드 접근 권한이 없어 **확인 못 함**.

## 태그 상태

`npm run build`가 108페이지로 정상 완료돼 `ALLOWED_TAGS` z.enum 스키마가 계속 지켜지고 있음을 확인했다. 특정 태그에 글이 1편만 남아있는 경우는 발견되지 않았다.

## 소견

발행 글(+3편)과 인스타그램 팔로워(+9명)·게시물(+3개) 모두 순조롭게 늘고 있다. 서치콘솔은 클릭이 28일 기준 정체(28로 동일)지만 노출(+436)과 평균순위(개선) 모두 긍정적으로 움직여, 08-15에 우려했던 "평균순위 악화 추세"는 이번 점검에서 반전됐다 — 하루 노이즈였던 것으로 보인다.

`family-relation-certificate-guide`의 핵심 키워드는 제목-검색어 일치에도 클릭이 없는데, 직접 확인 결과 원인은 문구가 아니라 순위(10위 전후) 자체이므로 추가 수정 없이 도메인 신뢰도 축적을 기다리는 게 맞다.

**종합 권고**: 전 지표가 개선 또는 순조로운 증가 추세이므로 "지금 이대로 유지"가 맞다. 쓰레드는 사장 지시로 계속 보류 상태.
