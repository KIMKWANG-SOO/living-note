# 생활정보노트 (living-note)

한국어 생활 정보 블로그. Astro 정적 사이트 → GitHub push → Vercel 자동 배포.
목표: 검색(구글·네이버) 유입을 만드는 부업 사이트. **매일 아침 글 3편 발행** + 사이트 점검.

## 매일 발행 루틴 — 작성 → 검수 → 수정 → 발행 → 카드뉴스 → 인스타 게시 (한 세션에서 끝까지 처리)

**핵심 원칙 1: 검수를 거치지 않은 글은 발행(git push)하지 않는다.** 발행 후 점검이 아니라 발행 전 점검이다.
**핵심 원칙 2 (시간 단축): 병렬로 할 수 있는 건 병렬로, 배포는 최대한 묶어서 1회로.**
- 글 3편은 순차가 아니라 병렬로 작성(Agent 도구로 blog-writer 3개 동시 호출).
- 카드뉴스도 글이 확정되는 즉시(발행 대기 없이 로컬 파일 기준으로) 병렬 제작(marketing 3개 동시 호출).
- **글 3편 + 카드뉴스 3세트를 한 번에 빌드·커밋·푸시** — 배포 트리거를 세션당 1번만 발생시킨다. 카드뉴스를 별도 예약 작업으로 분리하지 않는다(글 발행 완료 시점을 정확히 알 수 없어 대기 시간이 낭비되므로, 같은 세션에서 곧바로 이어서 처리).
- 인스타 게시는 배포 확인 후 순차로 진행.

1. **주제 선정**: `src/content/blog/`의 기존 글 목록과, 오늘 이미 쓴 편을 확인해 **중복되지 않는** 주제를 고른다. 3편은 서로 다른 카테고리로 분산한다.
   - 주제 범위(아래는 **글감을 고르는 참고용 범위일 뿐, 그대로 `tags`에 쓰는 값이 아니다**): 보험(청구·비교·용어), 민원 서류(정부24·홈택스·대법원 인터넷등기소), 정부 지원금·복지 제도, 자동차(검사·세금·보험), 부동산·전월세(계약·확정일자·전입신고), 건강보험·연말정산·세금, 통신·금융 생활 꿀팁, 해외직구(관세·통관·반품), 여행 준비(여권·국제운전면허증·여행자보험)
   - **`tags`에 넣을 값은 반드시 `src/content.config.ts`의 `ALLOWED_TAGS` 16개 중에서만 고른다.** 위 주제 범위 문구(예: "정부 지원금·복지 제도")를 그대로 태그로 쓰면 빌드가 실패한다 — 실제로 2026-08-10에 이 혼동으로 중복 태그가 생겨 태그가 47개까지 늘어난 적이 있다.
   - 검색 수요가 있는 "방법/절차/기한/비용" 형태의 주제를 우선한다.
2. **초안 작성**: `src/content/blog/영문-슬러그.md` 생성. 아래 품질 기준 준수. **이 단계에서는 커밋·푸시하지 않는다.**
3. **검수**: 3편을 CS팀 관점(비판적 제3자 시점)으로 다시 읽는다. 가능하면 cs 에이전트를 실제로 호출한다. 점검 항목: 사실관계 정확성(법률·제도 설명을 지나치게 단순화하지 않았는지), FAQ와 본문(특히 표)의 내용 일치, 품질 기준 충족 여부.
4. **수정**: 검수에서 발견된 확신 있는 오류는 즉시 고친다. 불확실한 항목은 단정하지 말고 "공식 사이트에서 확인" 문구로 헤지한다.
5. **점검(빌드)**: `npm run build` 실행. 실패하면 고친 뒤 재시도.
6. **발행**: 편마다 개별로 `git add -A && git commit && git push` → Vercel이 자동 배포 (한 편씩 커밋 — 중간 실패해도 이전 편은 발행된 채로 남도록).
7. **보고**: 3편 모두 마친 뒤, 발행한 글 제목·주제, **검수에서 찾아 고친 내용**, 빌드 결과를 요약해 남긴다. 검수를 실제로 거쳤다는 근거(무엇을 확인했는지)를 함께 남긴다.

## 글 품질 기준 (SEO)

- frontmatter: `title`(핵심 키워드 포함, 25~40자), `description`(80~140자, 검색 결과에 그대로 노출된다고 생각하고 작성), `pubDate`(오늘 날짜 YYYY-MM-DD)
- 본문 1,500자 이상. H2/H3로 구조화, 절차는 번호 목록, 비교는 표 사용
- 반드시 "자주 묻는 질문" 섹션 포함 (2~4개). **질문은 그 글에서만 나올 수 있는 것으로 만든다** — 아무 글에나 갖다 붙일 수 있는 일반론("신청은 어디서 하나요?" 수준)은 쓰지 않는다.
- 마지막에 공식 출처 안내 + 인용구(>)로 면책 문구. **면책 문구는 매번 같은 문장을 복사하지 말고 글 주제에 맞게 다시 쓴다**(무엇이 바뀔 수 있는지, 어느 기관에서 확인해야 하는지가 글마다 다르다).

### 획일성 회피 (2026-09-09 추가 — 애드센스 대응)

같은 틀로 찍어낸 글이 쌓이면 구글이 "가치가 별로 없는 콘텐츠"로 판정한다. 실제로 2026-09-09 점검에서 **158편 중 94편의 제목이 "총정리"로 끝나고**, FAQ 섹션과 맺음 면책 문구는 158편 전부가 같은 형태였다. 아래를 지킨다.

- **제목**: "○○ 총정리" 형태를 기본값으로 쓰지 않는다. 최근 발행한 10편의 제목을 먼저 확인하고, **끝맺음 패턴이 겹치면 다른 형태로 쓴다.** 쓸 수 있는 형태 예시 — 질문형("전세권 설정, 확정일자만으로는 부족할까?"), 상황형("번호판이 사라졌다면 도난이 아니라 영치일 수 있습니다"), 비교형("일반증명서와 상세증명서, 뭘 떼야 하나"), 기한 강조형("상속 취득세, 6개월을 넘기면 생기는 일"). 한 형태를 연달아 세 번 쓰지 않는다.
- **구성**: H2 제목과 순서를 매번 같게 만들지 않는다. 주제에 따라 비교표가 중심인 글, 시간 순서가 중심인 글, 오해 바로잡기가 중심인 글로 구조 자체를 달리 짠다.
- **도입부**: "○○하려면 막막하시죠" 류의 공감 문장 한 줄로 시작하는 패턴을 반복하지 않는다.
- **사실 검증 원칙**: 금액·기한·수수료 등 변동 가능한 수치는 단정하지 말고 "공식 사이트에서 확인" 안내를 병기한다. 확실하지 않은 수치는 쓰지 않는다. 의료·법률·투자 조언이 아닌 일반 정보 제공 톤을 유지한다.
- 슬러그는 영문 소문자-하이픈 (예: `year-end-tax-guide.md`)

## 쿠팡 파트너스 제휴 링크 규칙

- **링크는 사장이 파트너스에서 생성해 제공한 것만 사용한다** (`https://link.coupang.com/...` 형식). 에이전트가 임의로 쿠팡 URL을 만들지 않는다.
- **링크 저장소**: `marketing/coupang-links.md`에 사용 가능한 링크 목록이 있다. 새 글 작성 시 저장소를 확인해 **글 주제와 실제로 관련 있는 상품이 있으면 1개 삽입**하고, 없으면 링크 없이 발행한다.
- 제휴 링크가 들어간 글에는 **본문 맨 아래에 반드시 고지 문구**를 넣는다 (쿠팡 파트너스 의무 사항).
- 글 주제와 실제로 관련 있는 상품만 추천한다. 억지 끼워넣기 금지 (글당 **최대 1개**).
- **삽입 기준을 높인다 (2026-09-09 강화 — 애드센스 대응)**: 2026-09-09 기준 158편 중 84편(53%)에 제휴 박스가 들어가 있었고, 그중 상당수가 "서류 정리용 파일철" 같은 **주제와 느슨하게만 연결된 끼워넣기**였다. 앞으로는 **"그 글을 읽고 실제로 그 절차를 진행하는 사람이 그 물건을 살 이유가 있는가"**를 기준으로 판단하고, 애매하면 **넣지 않는다.** 하루 3편 중 제휴 박스가 들어가는 글은 1편 이하를 목표로 한다.
- **넣지 말아야 할 주제**: 보이스피싱·사기 피해, 질병·산정특례 등 의료, 사망·상속, 체납·압류처럼 독자가 곤란한 상황에 놓인 글에는 제휴 링크를 넣지 않는다.
- 삽입 패턴 (마크다운 글 안에 HTML로 삽입):

```html
<div class="product-box">
  <div class="product-label">🛒 함께 보면 좋은 준비물</div>
  <a href="쿠팡파트너스링크" target="_blank" rel="nofollow sponsored noopener">상품명</a>
  <p class="product-note">한 줄 추천 이유 (예: 검사 전 셀프 점검에 쓰는 타이어 공기압 게이지)</p>
</div>
```

- 고지 문구 (본문 마지막, 면책 인용구 위에):

```html
<p class="affiliate-disclosure">이 포스팅은 쿠팡 파트너스 활동의 일환으로, 이에 따른 일정액의 수수료를 제공받습니다.</p>
```

## 주간 점검 (매주 월요일 자동 실행)

매일 발행되는 글은 발행 전 자체 검수를 거치므로, 별도의 매일 팀 점검은 두지 않는다. 대신 **매주 월요일** 총괄 마스터가 CS(오래된 글 점검)·디자인(화면 개선)·개발(기술 점검) 3팀에 유지보수를 지시하고 검수·보고한다(예약 작업 `living-note-weekly-standup`).

## 명령어

- 개발 서버: `npm run dev` (localhost:4321)
- 빌드 점검: `npm run build`
- 미리보기: `npm run preview`

## 배포 규칙 (회사 매뉴얼 준수)

- **배포는 `git push origin main`으로만 한다.** Vercel CLI 등으로 직접 배포하지 말 것.
- 배포·서버 구성(Vercel 프로젝트 설정 등)은 기존 방식 그대로 유지하고 임의로 바꾸지 말 것.
- 작업 원칙: 정확성 > 검증 > 최소 변경. 수정 후 반드시 `npm run build`로 검증하고, 검증 없이 성공했다고 보고하지 않는다.

## 구조

- 글: `src/content/blog/*.md` (frontmatter 스키마: `src/content.config.ts`)
- 사이트 상수: `src/consts.ts`
- 레이아웃: `src/layouts/BlogPost.astro` (JSON-LD 포함)
- SEO: sitemap 자동 생성(@astrojs/sitemap), `public/robots.txt`, RSS(`src/pages/rss.xml.js`)
